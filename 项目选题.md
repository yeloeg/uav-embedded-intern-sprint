# 项目选题

岗位对口度 = 项目里有多少「飞控工程语言」。
所以选题不走通用嵌入式（智能家居 / 环境监测），直接锚定飞控。

## 主项目：基于 STM32 + FreeRTOS 的四旋翼飞控固件

**一句话承诺**：用 STM32F405 + FreeRTOS 写一个能稳定输出 1kHz 控制周期的四旋翼飞控固件，接 MAVLink 上 QGroundControl。

| 项目 | 内容 |
|---|---|
| 技术栈 | STM32F405/F411 + FreeRTOS + MPU6000 + DPS310 + SBUS + MAVLink + 串级 PID |
| 硬件 | 主控板 + 六轴 IMU + 气压计 + SBUS 接收机 + 电调电机 |
| 软件分层 | HAL → 驱动 → 算法 → 任务 → 通信 |
| 任务架构 | 姿态估计 1kHz / 控制 1kHz / 通信 50Hz / 日志 10Hz / 监控 1Hz |
| 核心算法 | 四元数姿态解算 + 互补滤波；串级 PID（角度环 + 角速度环） |
| 对外接口 | MAVLink（HEARTBEAT / ATTITUDE / RC_CHANNELS），QGC 遥测 |
| 验收标准 | 控制周期抖动 < 50μs；静态姿态误差 < 1°；有阶跃响应曲线；连续运行 1 小时不挂 |

**我的承诺（第 0 周填）**：____________________________________

## 辅项目：PX4 自定义模块开发 + SITL 仿真验证

**一句话承诺**：在 PX4 上写一个自定义模块，用 uORB 订阅传感器数据并发布控制指令，在 Gazebo SITL 里跑通定点悬停。

| 项目 | 内容 |
|---|---|
| 技术栈 | PX4 + NuttX + uORB + Gazebo / SITL |
| 环境 | Ubuntu + Gazebo + jMAVSim |
| 核心工作 | 编写自定义模块；理解 uORB 发布订阅；SITL 联合仿真 |
| 验收标准 | `make px4_sitl gz_x500` 跑通；自定义模块在仿真中生效；留 3 段录屏 |
| 附加产出 | PX4（NuttX 微内核）vs ArduPilot（ChibiOS 单体式）架构对比文档 |

**我的承诺（第 0 周填）**：____________________________________

## 为什么不是通用路线

| 维度 | 通用嵌入式路线 | 我选的飞控路线 |
|---|---|---|
| 主项目 | 环境监测节点 + OTA | 四旋翼飞控固件 + MAVLink + 地面站 |
| RTOS 用途 | 多任务数据采集 | 飞控任务调度（姿态估计 / 控制 / 通信） |
| 算法重点 | 软件滤波 | PID 串级控制 + EKF 姿态解算 |
| 通信协议 | 自定义帧 + CRC | MAVLink + CAN |
| 调试手段 | 逻辑分析仪 | 逻辑分析仪 + SITL 仿真 + 飞行测试 |
| 简历术语 | 「传感器采集」 | 「IMU 数据融合」「姿态解算」「控制律」 |

## 第 0 周自检

- [ ] 主项目一句话承诺写完了
- [ ] 辅项目一句话承诺写完了
- [ ] 两个项目都能一句话讲给外行听懂
- [ ] 确认硬件清单和预算（主项目板子 + IMU + 电机电调）
