# 简历弹药库

一句能写进简历的条目 = **技术栈 + 飞控场景 + 量化效果**。

**要求**：没有数字的条目先记下来，但标上 `【待量化】`，采集到数据再补。

## 已成型条目

| 编号 | 日期 | 项目 | 条目（可直接进简历） | 数据来源 |
|---|---|---|---|---|
| R-001 |  |  |  |  |

## 待量化条目

| 编号 | 待补数据 | 在哪里能拿到 |
|---|---|---|
|  |  |  |

---

## 主项目参考写法

**基于 STM32F405 + FreeRTOS 的四旋翼飞控固件**

- 技术栈：STM32F405 + FreeRTOS + MPU6000 + MAVLink + SBUS + 串级 PID
- 设计 **5 任务飞控架构**（姿态估计 1kHz / 控制 1kHz / 通信 50Hz / 日志 10Hz / 监控 1Hz），控制周期抖动 < 50μs
- 基于 MPU6000 实现 **四元数姿态解算 + 互补滤波**，静态姿态误差 < 1°
- 实现 **串级 PID 控制器**，通过阶跃响应测试将角速度响应时间从 __ms 优化至 __ms
- 集成 **MAVLink 协议**，与 QGroundControl 联调，遥测更新率 50Hz，丢包率 < 0.1%
- 用 `uxTaskGetStackHighWaterMark()` 优化任务栈，总 RAM 占用控制在 __KB 以内

## 辅项目参考写法

**PX4 自定义模块开发 + SITL 仿真验证**

- 技术栈：PX4 + NuttX + uORB + Gazebo / SITL
- 基于 PX4 框架开发 **自定义控制模块**，通过 uORB 订阅传感器数据并发布控制指令
- 在 **Gazebo SITL 仿真环境** 中验证模块功能，完成定点悬停与航点飞行测试
- 对比 PX4（NuttX 微内核）与 ArduPilot（ChibiOS 单体式）架构差异，撰写分析文档

## 12 周目标

- [ ] 收集 ≥ 20 条带量化数据的弹药
- [ ] 保证每条都能在面试里展开讲 3 分钟
