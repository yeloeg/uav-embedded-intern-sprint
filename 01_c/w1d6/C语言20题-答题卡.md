# W1D6 · C 语言面试题 20 道（飞控场景版）

**做法**：先合上所有资料自己写。写不出来先标 `【不会】`，**不要直接复制网上的答案**。
全部写完后再查资料补齐，补的内容标 `【查证】`。

**每题的答案控制在 3 行以内**，目标是面试时能 1 分钟讲完。
**验收标准**：20 题里至少 15 题能不查资料讲出来。

做完之后，把这份内容合并进仓库根目录的 `飞控面试题库.md`。

---

## 一、关键字与内存（1 - 5）

### Q1. `volatile` 的作用是什么？飞控里为什么用它修饰 IMU 数据缓冲区？

**考点**：编译器优化 / 中断与主循环共享变量

**答案**：

**能被追问的点**：

### Q2. `static` 修饰局部变量、全局变量、函数，三种情况分别是什么效果？

**考点**：生命周期 / 链接属性 / 作用域

**答案**：

**能被追问的点**：

### Q3. `const char *p`、`char * const p`、`const char * const p` 有什么区别？

**考点**：指针常量和常量指针

**答案**：

**能被追问的点**：

### Q4. 结构体内存对齐规则是什么？为什么 DMA 传输时要注意对齐？

**考点**：对齐 / padding / `__attribute__((packed))`

**答案**：

**能被追问的点**：

### Q5. 大端小端是什么？MAVLink 帧为什么规定用小端？代码里怎么处理？

**考点**：字节序 / 位域陷阱 / 网络字节序转换

**答案**：

**能被追问的点**：

---

## 二、指针与字符串（6 - 10）

### Q6. 指针和数组有什么区别？函数参数里的 `int a[]` 和 `int *a` 有区别吗？

**考点**：数组退化为指针 / `sizeof` 的陷阱

**答案**：

**能被追问的点**：

### Q7. `memcpy` 和 `memmove` 的区别？自己实现 `memcpy` 要注意什么？

**考点**：内存重叠 / 未定义行为 / 逐字节访问要用 `unsigned char*`

**答案**：

**能被追问的点**：

### Q8. `strcpy` 的返回值为什么设计成 `char *`？怎么防止缓冲区溢出？

**考点**：链式调用 / 边界检查 / `strncpy` 的坑

**答案**：

**能被追问的点**：

### Q9. 环形缓冲区怎么区分"空"和"满"？两种常见方案各自的取舍？

**考点**：`(head+1)%N == tail` / 计数器方案 / 为什么用 2 的幂

**答案**：

**能被追问的点**：

### Q10. 位操作：怎么只把寄存器的第 3 位置 1 而不影响其他位？宏的括号为什么不能省？

**考点**：`|=` `&=~` `^=` / 运算符优先级 / `1u` 而不是 `1`

**答案**：

**能被追问的点**：

---

## 三、函数与编译（11 - 15）

### Q11. 函数指针怎么声明和使用？命令表（表驱动）为什么比 `switch` 好维护？

**考点**：`void (*f)(int, char**)` 读法 / 解耦

**答案**：

**能被追问的点**：

### Q12. 编译器可能把 `while (!flag);` 优化掉吗？为什么？怎么避免？

**考点**：优化 / `volatile` / 循环里没有副作用的判断

**答案**：

**能被追问的点**：

### Q13. 局部变量、全局变量、静态变量分别存放在哪里？栈溢出会出现什么现象？

**考点**：`.text/.data/.bss` / 栈 / HardFault

**答案**：

**能被追问的点**：

### Q14. MCU 上为什么不推荐 `malloc` / `free`？FreeRTOS 的 `heap_4` 解决了什么问题？

**考点**：碎片 / 不确定性 / 内存池

**答案**：

**能被追问的点**：

### Q15. 编译的四个阶段分别做什么？`.map` 文件能告诉你什么信息？

**考点**：预处理 / 编译 / 汇编 / 链接 / 段布局

**答案**：

**能被追问的点**：

---

## 四、宏、中断与未定义行为（16 - 20）

### Q16. `extern` 的作用？在头文件里直接定义变量会发生什么？

**考点**：声明 vs 定义 / 多重定义错误

**答案**：

**能被追问的点**：

### Q17. 宏和函数的区别？带参数宏有哪些坑？什么场景该改用 `inline`？

**考点**：文本替换 / 副作用 / 类型检查

**答案**：

**能被追问的点**：

### Q18. 结构体指针访问 `p->x` 和 `(*p).x` 有区别吗？为什么要有 `->`？

**考点**：语法糖 / 优先级

**答案**：

**能被追问的点**：

### Q19. 中断服务函数里能调用 `printf` 和 `malloc` 吗？为什么？ISR 和主循环共享变量要注意什么？

**考点**：可重入 / 阻塞 / 临界区 / `volatile`

**答案**：

**能被追问的点**：

### Q20. 什么是未定义行为？举 3 个嵌入式里常见的例子。

**考点**：有符号溢出 / 越界访问 / 未初始化变量 / 严格别名 / 类型双关

**答案**：

**能被追问的点**：

---

## 收工自检

- [ ] 20 题全部写过一遍，没有空着的
- [ ] 标记了 `【不会】` 的题目，事后已经查证补上
- [ ] 至少 15 题能不查资料讲出来
- [ ] 内容已合并进 `飞控面试题库.md`
